The following example should be UNSATISFIABLE, because the multiplicity of the associations cannot be satisfied.

object of type CD@C will always need 2 objects of type AB@A, but creating an object of AB@A requires 1 object of CD@C -- and so the loop continues.