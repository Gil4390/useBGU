The following examples will test the credibility of the plugin with role removing feature.

Example 1: (SATISFIABLE) True Positive
Both 'A' and 'B' doesnt have any objects, the model is SATISFIABLE because the 1-1 multiplicity of the upper level doesn't exists in this diagram.

Example 2: (UNSATISFIABLE) True Negative
Class 'B' doesnt have any objects, the model is UNSATISFIABLE because object of type 'D' need to satisfy the 1-1 multiplicity of cd1 and ab1

