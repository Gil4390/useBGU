The following example, asserts that inherited roles and attributes
can be accessed through inter-constraints.

There is a soil file that can be used, open it in a text editor,
copy the lines to the command line, and in between each divider,
run the command 'check' to see if the current model satisfies or not.
